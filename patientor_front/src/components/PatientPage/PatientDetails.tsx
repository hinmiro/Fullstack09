import { Patient } from '../../types';

interface Props {
  patients: Patient[];
}

export const PatientDetails = ({ patients }: Props) => {
  return (
    <>
      <div>
        <p>TOIMII TÄNNE</p>
      </div>
    </>
  );
};
